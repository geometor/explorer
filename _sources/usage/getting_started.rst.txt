:order: 2

getting started
===============

To start the ``explorer`` server, run the following command in your terminal:

.. code-block:: bash

   explorer

This will launch a Flask server, usually accessible at ``http://127.0.0.1:4444``.

Open this URL in your web browser to access the interface.
